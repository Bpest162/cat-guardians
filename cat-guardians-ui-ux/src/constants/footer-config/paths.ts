import { ROUTES } from "../routing/urls";

export const desctopPaths = [ROUTES.LOGIN, ROUTES.SIGNUP, ROUTES.PETSLIST];

export const mobilePaths = [ROUTES.LOGIN, ROUTES.SIGNUP, ROUTES.PETSLIST, ROUTES.FOUND];
